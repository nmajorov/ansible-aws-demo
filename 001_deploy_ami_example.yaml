---
- name: "deploy centos9 to aws"
  hosts: "localhost"
  gather_facts: false
  connection: local
  become: false
  vars:
    ec2_instance_test_name: minimal
    
  roles:
    - { role: ec2_instance }
