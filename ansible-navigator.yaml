---
ansible-navigator:
  execution-environment:
    image: docker.io/nmajorov/nm-ansible-runner:3.1
    container-engine: podman
    #volume-mounts:
    #  - src: "~/.aws"
    #    dest: "/vars/aws"
    #    options: "Z"
